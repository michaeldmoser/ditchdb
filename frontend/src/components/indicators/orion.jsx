import cx from "@/utils/cx";

export default function Orion({ className, children }) {
  return (
    <span className="indicator-item badge badge-info text-info-content">
      orion
    </span>
  );
}
